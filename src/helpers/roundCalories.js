import React from 'react';

function RoundFunction({ roundCalories}) {
    function roundCalories(calories) {
        return Math.round(calories);
    }

    return (
        <div></div>
    );
}

export default RoundFunction;